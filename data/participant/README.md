# PARTICIPANT data boundary

This directory is a policy placeholder. Raw data are ignored by Git and must be stored only in the
approved data environment for this access class. Commit manifests, hashes, schemas, query definitions
and permitted result objects—not the raw dataset.
